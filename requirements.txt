# Cloudera ECS AI Ops — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29

# ── LLM / Agent ───────────────────────────────────────────────────────────────
langchain-core>=0.2
langgraph>=0.1

# ── LLM backend: HuggingFace Transformers (local model via --model-dir) ───────
# pip install -U langchain-huggingface transformers torch accelerate
# (listed separately as they require GPU/large-download setup)

# ── Kubernetes — Python API client (talks directly to remote cluster) ─────────
kubernetes>=29.0
pyyaml>=6.0           # used by kubectl_exec for YAML output formatting

# ── Vector DB: ChromaDB (embedded, no external server needed) ─────────────────
chromadb>=0.5

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=3.0
torch>=2.0

# ── Document parsing (Phase 2 RAG) ────────────────────────────────────────────
pypdf>=4.0
markdown-it-py>=3.0

# ── System metrics ────────────────────────────────────────────────────────────
psutil>=5.9
httpx>=0.27

# ── Config ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0

# ── GPU monitoring (NVIDIA only) ──────────────────────────────────────────────
nvidia-ml-py>=12.0
