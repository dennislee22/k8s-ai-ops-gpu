# Cloudera ECS AI Ops — Python dependencies
# Install: pip install -r requirements.txt
# No Node.js or npm required.

# Web framework
fastapi>=0.111
uvicorn[standard]>=0.29

# LLM / Agent
langchain-ollama>=0.1
langchain-core>=0.2
langgraph>=0.1

# Kubernetes
kubernetes>=29.0

# RAG / Vector DB (Phase 2)
psycopg2-binary>=2.9
httpx>=0.27

# Document parsing (Phase 2)
pypdf>=4.0
markdown-it-py>=3.0

# System metrics
psutil>=5.9

# Config
python-dotenv>=1.0

# GPU monitoring (optional — install only if NVIDIA GPU present)
# pip install nvidia-ml-py
