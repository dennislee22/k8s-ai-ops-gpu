# Cloudera ECS AI Ops — Python dependencies
# Install: pip install -r requirements.txt
# No Node.js, no PostgreSQL, no external vector DB required.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29

# ── LLM / Agent (Ollama-based) ────────────────────────────────────────────────
langchain-ollama>=0.1
langchain-core>=0.2
langgraph>=0.1

# ── Kubernetes ────────────────────────────────────────────────────────────────
kubernetes>=29.0

# ── Vector DB: ChromaDB (embedded, no external server needed) ─────────────────
chromadb>=0.5

# ── Embeddings: sentence-transformers (supports local dir via --embed-dir) ────
sentence-transformers>=3.0

# ── Document parsing (Phase 2 RAG) ───────────────────────────────────────────
pypdf>=4.0
markdown-it-py>=3.0

# ── System metrics ────────────────────────────────────────────────────────────
psutil>=5.9
httpx>=0.27

# ── Config ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0

# ── OPTIONAL: GPU monitoring ──────────────────────────────────────────────────
# pip install nvidia-ml-py

# ── OPTIONAL: Load LLM from local directory without Ollama (--model-dir) ──────
# pip install langchain-community transformers torch accelerate
